version: '3.7'

services:
  portfolio:
    container_name: portfolio

    build:
      context: .
      dockerfile: Dockerfile

    volumes:
      - '.:/app'
      - '/app/node_modules'

    ports:
      - '3000:3000'

    environment:
      - NODE_ENV=development
